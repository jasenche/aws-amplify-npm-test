version: 0.1
frontend:
  phases:
    build:
      commands:
        - ls -alR
        - cd webapp
        - npm install
        - npm run build
        - ls -alR
  artifacts:
    baseDirectory: webapp/build
    files:
      - '**/*'
  cache:
    paths:
      - webapp/node_modules/**/*
test:
  phases:
    test:
      commands:
        - ls -alR
        - cd webapp
        - CI=true npm test
        - ls -alR
  artifacts:
    baseDirectory: webapp/build
    files:
      - '**/*'
